# Деплой на Netlify

## 🚀 Быстрый деплой

### Вариант 1: Через Netlify UI (рекомендуется)

1. **Создайте аккаунт на Netlify**
   - Перейдите на https://netlify.com
   - Зарегистрируйтесь через GitHub

2. **Подключите Git репозиторий**
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите GitHub и найдите этот репозиторий
   - Netlify автоматически обнаружит `netlify.toml`

3. **Настройте environment variables**

   В Netlify UI перейдите в:
   `Site settings` → `Environment variables` → `Add a variable`

   Добавьте следующие переменные:

   ```
   GEMINI_API_KEY=your_gemini_api_key_here
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_ANON_KEY=your_supabase_anon_key_here
   ```

   ⚠️ **Важно**: Используйте значения из вашего `.env` файла

4. **Deploy!**
   - Нажмите "Deploy site"
   - Netlify автоматически запустит `npm run build`
   - Через 1-2 минуты сайт будет доступен

---

### Вариант 2: Через Netlify CLI

1. **Установите Netlify CLI**
   ```bash
   npm install -g netlify-cli
   ```

2. **Авторизуйтесь**
   ```bash
   netlify login
   ```

3. **Инициализируйте проект**
   ```bash
   netlify init
   ```

4. **Добавьте environment variables**
   ```bash
   netlify env:set GEMINI_API_KEY "your_key_here"
   netlify env:set SUPABASE_URL "https://your-project.supabase.co"
   netlify env:set SUPABASE_ANON_KEY "your_anon_key_here"
   ```

5. **Deploy**
   ```bash
   netlify deploy --prod
   ```

---

## ⚙️ Конфигурация

Файл `netlify.toml` уже настроен:

```toml
[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### Что это означает:
- **publish**: папка с собранным приложением
- **command**: команда для сборки
- **NODE_VERSION**: версия Node.js для сборки
- **redirects**: все маршруты перенаправляются на index.html (для SPA)

---

## 🔐 Environment Variables

### Где взять значения:

#### 1. GEMINI_API_KEY
- Перейдите на https://aistudio.google.com/app/apikey
- Создайте новый API ключ
- Скопируйте значение

#### 2. SUPABASE_URL и SUPABASE_ANON_KEY
- Откройте ваш проект в https://app.supabase.com
- Перейдите в `Settings` → `API`
- Скопируйте:
  - **Project URL** → `SUPABASE_URL`
  - **anon/public key** → `SUPABASE_ANON_KEY`

---

## 🔄 Автоматический деплой

После первого деплоя Netlify будет автоматически:
- ✅ Деплоить при каждом push в main/master ветку
- ✅ Создавать preview для Pull Requests
- ✅ Показывать логи сборки
- ✅ Откатывать на предыдущую версию если нужно

---

## 📊 После деплоя

### Проверьте работу дашборда:

1. **Откройте URL** (например, `https://your-site-name.netlify.app`)

2. **Проверьте консоль браузера** (F12):
   ```
   ✅ Supabase client initialized
   📡 Loading data from Supabase...
   ⚠️ Snapshots empty or all zeros, aggregating from events
   📊 Aggregating metrics from 217 events
   ✅ Aggregated metrics for 9 days
   ```

3. **Проверьте данные**:
   - Должны загрузиться метрики из Supabase
   - Комментарии должны отображаться
   - Графики должны показывать данные

### Если что-то не работает:

1. **Проверьте логи сборки** в Netlify UI
2. **Проверьте environment variables**
3. **Проверьте консоль браузера** на ошибки
4. **Проверьте подключение к Supabase**:
   - Правильный ли URL?
   - Правильный ли API ключ?
   - Есть ли данные в базе?

---

## 🎨 Custom Domain (опционально)

Если хотите свой домен:

1. Купите домен (Namecheap, GoDaddy, etc.)
2. В Netlify UI: `Domain settings` → `Add custom domain`
3. Следуйте инструкциям по настройке DNS
4. Netlify автоматически настроит HTTPS (Let's Encrypt)

---

## 📈 Мониторинг

Netlify предоставляет бесплатно:
- 📊 Analytics (количество посещений)
- ⚡ Performance metrics
- 🔍 Deploy logs
- 📧 Email уведомления о деплоях

---

## ✅ Checklist перед деплоем

- [ ] Build проходит локально (`npm run build`)
- [ ] `.env` файл НЕ закоммичен в git
- [ ] Environment variables настроены в Netlify
- [ ] `netlify.toml` присутствует
- [ ] Supabase database содержит данные
- [ ] Git репозиторий подключен к Netlify

---

## 🐛 Troubleshooting

### Ошибка: "Build failed"
- Проверьте логи в Netlify UI
- Убедитесь что все зависимости в `package.json`
- Проверьте версию Node.js в `netlify.toml`

### Ошибка: "Page not found" (404)
- Проверьте что `redirects` настроены в `netlify.toml`
- Убедитесь что `publish = "dist"` правильно

### Данные не загружаются
- Проверьте environment variables в Netlify
- Откройте консоль браузера (F12) и проверьте ошибки
- Проверьте что Supabase URL и ключ правильные

### Большой размер bundle (964 KB)
- Это нормально для первой версии
- Можно оптимизировать позже через code splitting
- Netlify использует CDN, загрузка будет быстрой

---

## 🎉 Готово!

После деплоя дашборд будет доступен 24/7 на вашем Netlify URL.

**Следующий шаг**: Настроить Orchestrator для автоматического сбора данных.
