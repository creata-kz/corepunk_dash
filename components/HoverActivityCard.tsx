// This component is no longer in use.
// Its functionality has been replaced by the ActivityDetailModal.
// Keeping the file to avoid breaking potential build processes, but it can be safely removed.
import React from 'react';
export const HoverActivityCard: React.FC = () => null;
